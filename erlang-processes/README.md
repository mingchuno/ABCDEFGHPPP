### Erlang (Dirty) Program for (AB - CD = EF) + GH = PPP
Alternative, Parallel Approach

### Prerequisite
**Install Erlang (Ubuntu)**
```bash
$ sudo apt-get install erlang
```

### Usage
```bash
$ chmod +x ppp.erl
$ ./ppp.erl
```
